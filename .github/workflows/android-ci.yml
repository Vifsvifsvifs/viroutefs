name: Android CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag, for example v0.13.0-beta.1'
        required: true
        type: string
      release_name:
        description: 'Release title, for example ViRouteFS 0.13.0-beta.1'
        required: true
        type: string
      prerelease:
        description: 'Mark this GitHub Release as a prerelease'
        required: true
        default: true
        type: boolean

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build debug APK
    runs-on: ubuntu-latest
    outputs:
      version_name: ${{ steps.prepare_apk.outputs.version_name }}
      version_code: ${{ steps.prepare_apk.outputs.version_code }}
    permissions:
      contents: read

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Prepare release signing key
        env:
          VIROUTEFS_ALPHA_KEYSTORE_BASE64: ${{ secrets.VIROUTEFS_ALPHA_KEYSTORE_BASE64 }}
          VIROUTEFS_ALPHA_KEYSTORE_PASSWORD: ${{ secrets.VIROUTEFS_ALPHA_KEYSTORE_PASSWORD }}
          VIROUTEFS_ALPHA_KEY_ALIAS: ${{ secrets.VIROUTEFS_ALPHA_KEY_ALIAS }}
          VIROUTEFS_ALPHA_KEY_PASSWORD: ${{ secrets.VIROUTEFS_ALPHA_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          if [ -n "${VIROUTEFS_ALPHA_KEYSTORE_BASE64:-}" ]; then
            RELEASE_KEYSTORE_PATH="$RUNNER_TEMP/viroutefs-beta.jks"
            printf '%s' "$VIROUTEFS_ALPHA_KEYSTORE_BASE64" | base64 --decode > "$RELEASE_KEYSTORE_PATH"
            {
              echo "VIROUTEFS_ALPHA_KEYSTORE_PATH=$RELEASE_KEYSTORE_PATH"
              echo "VIROUTEFS_ALPHA_KEYSTORE_PASSWORD=$VIROUTEFS_ALPHA_KEYSTORE_PASSWORD"
              echo "VIROUTEFS_ALPHA_KEY_ALIAS=$VIROUTEFS_ALPHA_KEY_ALIAS"
              echo "VIROUTEFS_ALPHA_KEY_PASSWORD=$VIROUTEFS_ALPHA_KEY_PASSWORD"
            } >> "$GITHUB_ENV"
            echo "Beta signing key decoded into RUNNER_TEMP."
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "A manual release requires VIROUTEFS_ALPHA_KEYSTORE_* repository secrets." >&2
            exit 1
          else
            echo "VIROUTEFS_ALPHA_KEYSTORE_BASE64 is not configured; building with default debug signing."
          fi

      - name: Run tests and build APK
        run: |
          set -euo pipefail
          chmod +x ./gradlew
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            ./gradlew :app:testDebugUnitTest :app:lintDebug :app:assembleRelease --stacktrace
          else
            ./gradlew :app:testDebugUnitTest :app:lintDebug :app:assembleDebug --stacktrace
          fi

      - name: Prepare APK artifact
        id: prepare_apk
        run: |
          set -euo pipefail

          VERSION_NAME="$(./gradlew -q :app:printVersionName | tail -n 1)"
          VERSION_CODE="$(./gradlew -q :app:printVersionCode | tail -n 1)"
          if [ -z "$VERSION_NAME" ]; then
            echo "Unable to obtain versionName from Gradle." >&2
            exit 1
          fi

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            APK_DIRECTORY="app/build/outputs/apk/release"
            BUILD_KIND="release"
          else
            APK_DIRECTORY="app/build/outputs/apk/debug"
            BUILD_KIND="debug"
          fi

          mapfile -t APKS < <(find "$APK_DIRECTORY" -maxdepth 1 -type f -name '*.apk' | sort)
          if [ "${#APKS[@]}" -eq 0 ]; then
            echo "No APK found under $APK_DIRECTORY." >&2
            find "$APK_DIRECTORY" -maxdepth 2 -print >&2 || true
            exit 1
          fi

          SELECTED_APK="${APKS[0]}"
          ARTIFACT_NAME="ViRouteFS-${BUILD_KIND}-${VERSION_NAME}"
          ARTIFACT_APK="${ARTIFACT_NAME}.apk"
          RELEASE_APK="ViRouteFS-${VERSION_NAME}.apk"

          echo "Detected VERSION_NAME: ${VERSION_NAME}"
          echo "Selected APK path: ${SELECTED_APK}"
          echo "CI artifact filename: ${ARTIFACT_APK}"
          echo "Release asset filename: ${RELEASE_APK}"

          cp "$SELECTED_APK" "$ARTIFACT_APK"
          cp "$SELECTED_APK" "$RELEASE_APK"
          {
            echo "VERSION_NAME=${VERSION_NAME}"
            echo "VERSION_CODE=${VERSION_CODE}"
            echo "ARTIFACT_NAME=${ARTIFACT_NAME}"
            echo "ARTIFACT_APK=${ARTIFACT_APK}"
            echo "RELEASE_APK=${RELEASE_APK}"
          } >> "$GITHUB_ENV"
          {
            echo "version_name=${VERSION_NAME}"
            echo "version_code=${VERSION_CODE}"
          } >> "$GITHUB_OUTPUT"

      - name: Verify signed release APK
        if: github.event_name == 'workflow_dispatch'
        env:
          EXPECTED_XRAY_SHA256: 9bb0b815086395164066b5fa27b1797bf9a0fcc493d1491f02166560604dcaff
          XRAY_COMMIT: 94ffd50060f1cfd5d7482ec90a23a92bdefdff68
          EXPECTED_ZAPRET2_SHA256: 2e1a0e950e0bc7189b5662e54fdd66d749d51215b167a647f15659554e7b4090
          ZAPRET2_RELEASE: v1.0.4
          ZAPRET2_COMMIT: 2c21faa80e1acb71ddceb8b49176f266b7d33f05
          EXPECTED_TCPDUMP_SHA256: adb46aa539d42efb6d07c1afc42edc39954fd59a46c09561411bb98bb176c4da
          TCPDUMP_RELEASE: '4.99.6'
          LIBPCAP_RELEASE: '1.10.6'
          WIREGUARD_ANDROID_VERSION: '1.0.20260102'
          WIREGUARD_ANDROID_COMMIT: 09b75c2bd37f749e2a8c85876394854113c74be7
          WIREGUARD_TOOLS_COMMIT: e2ecaaa739144997ccff89d6ad6ec81698ea6ced
          WIREGUARD_TOOLS_SOURCE_SHA256: b5f838a044e9daa19eb011524efd813d12997fe5b44c1224a15cde4e99c10f75
          EXPECTED_WG_SHA256: f68dfc2c95e0326e04bf427b055f729f3a51e840f78c8ad177259d42ab2722bc
          EXPECTED_WG_QUICK_SHA256: a032456fbd3b8e577f6b4026ae049c4e7c9056cb7511f77c059e059394cb9092
          EXPECTED_WG_GO_SHA256: 68730e8c81b613113249574e22838fd6ce9b1c4da2be2f5fe7c71d651e32b005
        run: |
          set -euo pipefail

          BUILD_TOOLS_DIR="$(find "$ANDROID_HOME/build-tools" -mindepth 1 -maxdepth 1 -type d | sort -V | tail -n 1)"
          APKSIGNER="$BUILD_TOOLS_DIR/apksigner"
          ZIPALIGN="$BUILD_TOOLS_DIR/zipalign"
          SHA256="$(sha256sum "$RELEASE_APK" | awk '{print $1}')"

          "$APKSIGNER" verify --verbose --print-certs "$RELEASE_APK" | tee apk-verification.txt
          "$ZIPALIGN" -c -P 16 4 "$RELEASE_APK"
          unzip -l "$RELEASE_APK" | grep -q 'lib/arm64-v8a/'
          unzip -l "$RELEASE_APK" | grep -q 'assets/licenses/Xray-core-MPL-2.0.txt'
          unzip -l "$RELEASE_APK" | grep -q 'assets/licenses/zapret2-MIT.txt'
          unzip -l "$RELEASE_APK" | grep -q 'assets/licenses/tcpdump-libpcap-BSD.txt'
          unzip -l "$RELEASE_APK" | grep -q 'assets/licenses/WireGuard-tools-GPL-2.0.txt'
          unzip -l "$RELEASE_APK" | grep -q 'assets/licenses/WireGuard-go-MIT.txt'
          unzip -l "$RELEASE_APK" | grep -q 'assets/zapret2/zapret-lib.lua'
          unzip -l "$RELEASE_APK" | grep -q 'assets/zapret2/zapret-antidpi.lua'
          unzip -l "$RELEASE_APK" | grep -q 'assets/zapret2/zapret-auto.lua'
          SOURCE_XRAY_SHA256="$(sha256sum app/src/main/jniLibs/arm64-v8a/libxray.so | awk '{print $1}')"
          unzip -p "$RELEASE_APK" 'lib/arm64-v8a/libxray.so' > "$RUNNER_TEMP/libxray-packaged.so"
          PACKAGED_XRAY_SHA256="$(sha256sum "$RUNNER_TEMP/libxray-packaged.so" | awk '{print $1}')"
          SOURCE_ZAPRET2_SHA256="$(sha256sum app/src/main/jniLibs/arm64-v8a/libzapret2.so | awk '{print $1}')"
          unzip -p "$RELEASE_APK" 'lib/arm64-v8a/libzapret2.so' > "$RUNNER_TEMP/libzapret2-packaged.so"
          PACKAGED_ZAPRET2_SHA256="$(sha256sum "$RUNNER_TEMP/libzapret2-packaged.so" | awk '{print $1}')"
          SOURCE_TCPDUMP_SHA256="$(sha256sum app/src/main/jniLibs/arm64-v8a/libtcpdump.so | awk '{print $1}')"
          unzip -p "$RELEASE_APK" 'lib/arm64-v8a/libtcpdump.so' > "$RUNNER_TEMP/libtcpdump-packaged.so"
          PACKAGED_TCPDUMP_SHA256="$(sha256sum "$RUNNER_TEMP/libtcpdump-packaged.so" | awk '{print $1}')"
          unzip -p "$RELEASE_APK" 'lib/arm64-v8a/libwg.so' > "$RUNNER_TEMP/libwg-packaged.so"
          PACKAGED_WG_SHA256="$(sha256sum "$RUNNER_TEMP/libwg-packaged.so" | awk '{print $1}')"
          unzip -p "$RELEASE_APK" 'lib/arm64-v8a/libwg-quick.so' > "$RUNNER_TEMP/libwg-quick-packaged.so"
          PACKAGED_WG_QUICK_SHA256="$(sha256sum "$RUNNER_TEMP/libwg-quick-packaged.so" | awk '{print $1}')"
          unzip -p "$RELEASE_APK" 'lib/arm64-v8a/libwg-go.so' > "$RUNNER_TEMP/libwg-go-packaged.so"
          PACKAGED_WG_GO_SHA256="$(sha256sum "$RUNNER_TEMP/libwg-go-packaged.so" | awk '{print $1}')"
          if [ "$SOURCE_XRAY_SHA256" != "$EXPECTED_XRAY_SHA256" ]; then
            echo "Pinned Xray source hash mismatch: $SOURCE_XRAY_SHA256" >&2
            exit 1
          fi
          if [ "$PACKAGED_XRAY_SHA256" != "$EXPECTED_XRAY_SHA256" ]; then
            echo "Packaged Xray hash mismatch: $PACKAGED_XRAY_SHA256" >&2
            exit 1
          fi
          if [ "$SOURCE_ZAPRET2_SHA256" != "$EXPECTED_ZAPRET2_SHA256" ]; then
            echo "Pinned zapret2 source hash mismatch: $SOURCE_ZAPRET2_SHA256" >&2
            exit 1
          fi
          if [ "$PACKAGED_ZAPRET2_SHA256" != "$EXPECTED_ZAPRET2_SHA256" ]; then
            echo "Packaged zapret2 hash mismatch: $PACKAGED_ZAPRET2_SHA256" >&2
            exit 1
          fi
          if [ "$SOURCE_TCPDUMP_SHA256" != "$EXPECTED_TCPDUMP_SHA256" ]; then
            echo "Pinned tcpdump source hash mismatch: $SOURCE_TCPDUMP_SHA256" >&2
            exit 1
          fi
          if [ "$PACKAGED_TCPDUMP_SHA256" != "$EXPECTED_TCPDUMP_SHA256" ]; then
            echo "Packaged tcpdump hash mismatch: $PACKAGED_TCPDUMP_SHA256" >&2
            exit 1
          fi
          if [ "$PACKAGED_WG_SHA256" != "$EXPECTED_WG_SHA256" ]; then
            echo "Packaged WireGuard wg hash mismatch: $PACKAGED_WG_SHA256" >&2
            exit 1
          fi
          if [ "$PACKAGED_WG_QUICK_SHA256" != "$EXPECTED_WG_QUICK_SHA256" ]; then
            echo "Packaged WireGuard wg-quick hash mismatch: $PACKAGED_WG_QUICK_SHA256" >&2
            exit 1
          fi
          if [ "$PACKAGED_WG_GO_SHA256" != "$EXPECTED_WG_GO_SHA256" ]; then
            echo "Packaged WireGuard wireguard-go hash mismatch: $PACKAGED_WG_GO_SHA256" >&2
            exit 1
          fi
          WIREGUARD_TOOLS_SOURCE_ARCHIVE="wireguard-tools-${WIREGUARD_TOOLS_COMMIT}.tar.gz"
          curl --fail --location --retry 3 \
            --output "$WIREGUARD_TOOLS_SOURCE_ARCHIVE" \
            "https://github.com/WireGuard/wireguard-tools/archive/${WIREGUARD_TOOLS_COMMIT}.tar.gz"
          DOWNLOADED_WIREGUARD_TOOLS_SOURCE_SHA256="$(sha256sum "$WIREGUARD_TOOLS_SOURCE_ARCHIVE" | awk '{print $1}')"
          if [ "$DOWNLOADED_WIREGUARD_TOOLS_SOURCE_SHA256" != "$WIREGUARD_TOOLS_SOURCE_SHA256" ]; then
            echo "WireGuard tools source archive hash mismatch: $DOWNLOADED_WIREGUARD_TOOLS_SOURCE_SHA256" >&2
            exit 1
          fi
          printf '%s  %s\n' "$SHA256" "$RELEASE_APK" > "$RELEASE_APK.sha256"

          CERT_SHA256="$(sed -n 's/^.*certificate SHA-256 digest: //p' apk-verification.txt | head -n 1)"
          if ! printf '%s' "$CERT_SHA256" | grep -Eq '^[0-9a-fA-F]{64}$'; then
            echo "Could not extract the signing certificate SHA-256 digest." >&2
            exit 1
          fi
          cat > release-manifest.json <<EOF
          {
            "versionName": "${VERSION_NAME}",
            "versionCode": ${VERSION_CODE},
            "commit": "${GITHUB_SHA}",
            "apk": "${RELEASE_APK}",
            "sha256": "${SHA256}",
            "certificateSha256": "${CERT_SHA256}",
            "abi": ["arm64-v8a"],
            "minSdk": 26,
            "targetSdk": 36,
            "zipalign16KiB": true,
            "nativeComponents": {
              "xrayCore": {
                "commit": "${XRAY_COMMIT}",
                "sha256": "${PACKAGED_XRAY_SHA256}",
                "license": "MPL-2.0"
              },
              "zapret2": {
                "release": "${ZAPRET2_RELEASE}",
                "commit": "${ZAPRET2_COMMIT}",
                "sha256": "${PACKAGED_ZAPRET2_SHA256}",
                "license": "MIT",
                "rootOptional": true,
                "deviceVerified": false
              },
              "tcpdumpLibpcap": {
                "tcpdumpRelease": "${TCPDUMP_RELEASE}",
                "libpcapRelease": "${LIBPCAP_RELEASE}",
                "sha256": "${PACKAGED_TCPDUMP_SHA256}",
                "license": "BSD-3-Clause",
                "rootOptional": true,
                "pageAlignmentKiB": 16,
                "deviceVerified": false
              },
              "wireGuardAndroid": {
                "version": "${WIREGUARD_ANDROID_VERSION}",
                "commit": "${WIREGUARD_ANDROID_COMMIT}",
                "wgSha256": "${PACKAGED_WG_SHA256}",
                "wgQuickSha256": "${PACKAGED_WG_QUICK_SHA256}",
                "wireguardGoSha256": "${PACKAGED_WG_GO_SHA256}",
                "wireguardToolsSourceCommit": "${WIREGUARD_TOOLS_COMMIT}",
                "wireguardToolsSourceArchive": "${WIREGUARD_TOOLS_SOURCE_ARCHIVE}",
                "wireguardToolsSourceSha256": "${DOWNLOADED_WIREGUARD_TOOLS_SOURCE_SHA256}",
                "licenses": ["Apache-2.0", "GPL-2.0", "MIT"],
                "rootOptional": true,
                "pageAlignmentKiB": 16,
                "deviceVerified": false
              }
            }
          }
          EOF

      - name: Upload debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_APK }}
          if-no-files-found: error

      - name: Upload release APK artifact
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: ViRouteFS-release-apk
          path: |
            ${{ env.RELEASE_APK }}
            ${{ env.RELEASE_APK }}.sha256
            release-manifest.json
            apk-verification.txt
            wireguard-tools-e2ecaaa739144997ccff89d6ad6ec81698ea6ced.tar.gz
          if-no-files-found: error

  publish-release:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'workflow_dispatch'
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Download release APK artifact
        uses: actions/download-artifact@v4
        with:
          name: ViRouteFS-release-apk
          path: release-apk

      - name: Prepare release notes
        run: |
          set -euo pipefail

          VERSION_NAME="${{ needs.build.outputs.version_name }}"
          VERSION_CODE="${{ needs.build.outputs.version_code }}"
          RELEASE_APK="ViRouteFS-${VERSION_NAME}.apk"

          if [ ! -f "release-apk/${RELEASE_APK}" ]; then
            echo "Expected release APK not found: release-apk/${RELEASE_APK}" >&2
            find release-apk -maxdepth 2 -type f -print >&2 || true
            exit 1
          fi

          EXPECTED_TAG="v${VERSION_NAME}"
          if [ "${{ inputs.tag }}" != "$EXPECTED_TAG" ]; then
            echo "Release tag must be $EXPECTED_TAG for this APK, got ${{ inputs.tag }}." >&2
            exit 1
          fi

          SHA256="$(sha256sum "release-apk/${RELEASE_APK}" | awk '{print $1}')"
          grep -q "^${SHA256}  ${RELEASE_APK}$" "release-apk/${RELEASE_APK}.sha256"
          DOC_VERSION="$(printf '%s' "$VERSION_NAME" | sed -E 's/-beta[.-]?([0-9]+)/_BETA_\1/I')"
          SOURCE_NOTES="docs/RELEASE_NOTES_${DOC_VERSION}.md"

          cat > release-notes.md <<EOF
          ## ViRouteFS ${VERSION_NAME}

          - versionName: ${VERSION_NAME}
          - versionCode: ${VERSION_CODE}
          - SHA-256: \`${SHA256}\`

          This is a manually approved, consistently signed beta build from the tagged source.

          Beta update behavior:
          - The app supports manual update checking from Settings.
          - APK download starts only after the user taps Download APK.
          - Install update opens Android's system package installer.
          - No background checks.
          - No auto-download.
          - No silent install.
          - Android / Play Protect may show sideload warnings outside Google Play; ViRouteFS cannot suppress system install warnings.

          If an older APK used a different signing key, Android requires uninstalling it once before this new signing line can be installed.
          EOF
          if [ -f "$SOURCE_NOTES" ]; then
            {
              printf '\n---\n\n'
              cat "$SOURCE_NOTES"
            } >> release-notes.md
          fi

          {
            echo "VERSION_NAME=${VERSION_NAME}"
            echo "RELEASE_APK=${RELEASE_APK}"
          } >> "$GITHUB_ENV"

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ inputs.tag }}
          RELEASE_NAME: ${{ inputs.release_name }}
          PRERELEASE: ${{ inputs.prerelease }}
        run: |
          set -euo pipefail

          args=(
            "$RELEASE_TAG"
            "release-apk/$RELEASE_APK"
            "release-apk/$RELEASE_APK.sha256"
            "release-apk/release-manifest.json"
            "release-apk/apk-verification.txt"
            "release-apk/wireguard-tools-e2ecaaa739144997ccff89d6ad6ec81698ea6ced.tar.gz"
            --title "$RELEASE_NAME"
            --notes-file release-notes.md
            --target "$GITHUB_SHA"
          )
          if [ "$PRERELEASE" = "true" ]; then
            args+=(--prerelease)
          fi

          gh release create "${args[@]}"

          TAG_SHA="$(gh api "repos/${GITHUB_REPOSITORY}/git/ref/tags/${RELEASE_TAG}" --jq '.object.sha')"
          if [ "$TAG_SHA" != "$GITHUB_SHA" ]; then
            echo "Release tag points to $TAG_SHA instead of the built commit $GITHUB_SHA." >&2
            exit 1
          fi
